std::ostream& operator<<(std::ostream& out, Stack& stack)
{
    out << "[ ";
    Stack::Nodo* aux;
    aux = stack.top;
    while(aux != nullptr)
    {
        out << aux->dato << " ";
        aux = aux->prev;
    }
    out << " ]";
    return out;
}
